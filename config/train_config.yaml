# VAE Training Configuration

# Data settings
data:
  data_dir: "/mnt/tim/VAE_for_Pneumonia/chest_xray"  # Path to chest_xray dataset directory
  image_size: 224           # Image size (will be resized to image_size x image_size)
  num_workers: 4            # Number of data loading workers

# Model settings
model:
  latent_dim: 128           # Latent dimension size

# Training settings
training:
  batch_size: 32            # Batch size for training
  epochs: 120        # Number of training epochs
  lr: 0.001                 # Learning rate (1e-3)
  kl_weight: 0.1          # Weight for KL divergence loss
  reduction: "mean"         # Reduction type for loss calculation

# Output settings
output:
  output_dir: "/mnt/tim/VAE_for_Pneumonia/outputs/mean_0.1"   # Directory to save outputs (checkpoints, samples, plots)
  save_interval: 10         # Save checkpoint every N epochs
  sample_interval: 5        # Generate sample images every N epochs

# Device settings
device:
  cuda: true                # Use CUDA if available (set to false to force CPU)

# Weights & Biases settings
wandb:
  use_wandb: true           # Enable Weights & Biases logging
  project: "vae-pneumonia"  # Wandb project name
  entity: kttu_tim-uc-san-diego           # Wandb entity/username (set to your wandb username)
  name: pneumonia-vae                # Wandb run name (leave null for auto-generated name)

